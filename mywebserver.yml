---
- hosts: all
  become:  yes
  gather_facts: yes
  tasks:
  - name: i want to install webserver on ansible node
    apt:
     name: apache2
     state: present

  - name: i want to start my webserver
    service: 
     name: apache2
     state: started